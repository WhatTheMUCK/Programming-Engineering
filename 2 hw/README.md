# Отчёт по домашнему заданию №2: REST API сервис электронной почты

**Студент группы М8О-106СВ-21:** Меркулов Фёдор Алексеевич  
**Вариант:** №9 — Электронная почта

---

## Что было сделано

Реализовал REST API для упрощённого почтового сервиса на C++ с использованием фреймворка Yandex uServer. Получилась микросервисная архитектура с тремя независимыми сервисами, объединёнными через Nginx.
М

Для быстрого запуска используйте инструкцию `QUICK_START.md`

### Архитектура решения

Вместо монолита сделал разделение по доменам:

- **User Service** (порт 8081) — управление пользователями и аутентификация
- **Folder Service** (порт 8082) — работа с почтовыми папками
- **Message Service** (порт 8083) — операции с письмами
- **Nginx** (порт 8080) — API Gateway, единая точка входа

Такой подход даёт гибкость: каждый сервис можно масштабировать отдельно, а если один упадёт, остальные продолжат работать.

### Реализованные API endpoints

Все 8 требуемых операций покрыты:

**Пользователи:**
- `POST /api/v1/users` — регистрация нового пользователя
- `POST /api/v1/auth/login` — вход и получение JWT токена
- `GET /api/v1/users/by-login?login=...` — поиск по логину (требует авторизации)
- `GET /api/v1/users/search?mask=...` — поиск по маске имени/фамилии (требует авторизации)

**Папки:**
- `POST /api/v1/folders` — создание папки (требует авторизации)
- `GET /api/v1/folders` — список всех папок пользователя (требует авторизации)

**Письма:**
- `POST /api/v1/folders/{folderId}/messages` — создание письма в папке (требует авторизации)
- `GET /api/v1/folders/{folderId}/messages` — список писем в папке (требует авторизации)
- `GET /api/v1/messages/{messageId}` — получение письма по ID (требует авторизации)

### Технические детали

**Стек технологий:**
- C++20 для основного кода
- Yandex uServer как веб-фреймворк
- SQLite для хранения данных (с WAL-режимом для конкурентности)
- JWT-CPP для работы с токенами
- Docker + Docker Compose для контейнеризации
- Nginx как reverse proxy
- Python + pytest для интеграционных тестов

**Аутентификация:**
Реализовал JWT-based аутентификацию. При логине пользователь получает токен, который живёт 24 часа. Токен содержит `user_id` и подписан секретным ключом. Защищённые endpoints проверяют токен через кастомный `JwtAuthChecker`, который:
1. Извлекает токен из заголовка `Authorization: Bearer <token>`
2. Проверяет подпись и срок действия
3. Достаёт `user_id` и кладёт в контекст запроса

Это позволяет каждому handler'у знать, кто делает запрос, и проверять права доступа.

**Модели данных:**
Три основные сущности с чёткими связями:
- `User` — пользователь (логин, email, имя, хеш пароля)
- `Folder` — папка (привязана к пользователю)
- `Message` — письмо (привязано к папке и отправителю)

Пароли хешируются через SHA-256 перед сохранением. В базе есть индексы на часто используемые поля для быстрого поиска.

**Обработка ошибок:**
Использую правильные HTTP статус-коды:
- `200 OK` — успешное получение данных
- `201 Created` — успешное создание ресурса
- `400 Bad Request` — невалидные данные (пустые поля, неправильный email)
- `401 Unauthorized` — отсутствует или невалидный токен
- `403 Forbidden` — нет прав доступа к ресурсу
- `404 Not Found` — ресурс не найден
- `409 Conflict` — конфликт (например, логин уже занят)

Каждая ошибка возвращает JSON с полем `error` и понятным описанием.

**Валидация:**
На уровне handler'ов проверяю:
- Наличие обязательных полей
- Формат email (должен содержать @)
- Непустые строки
- Права доступа (папка/письмо принадлежит пользователю)

**Безопасность:**
- Пароли не возвращаются в API ответах
- Проверка владения ресурсами (пользователь может видеть только свои папки и письма)
- JWT токены с ограниченным сроком жизни

### Структура проекта

```
email-service/
├── src/
│   ├── user/          # User Service
│   │   ├── main.cpp
│   │   ├── handlers.cpp
│   │   └── handlers.hpp
│   ├── folder/        # Folder Service
│   │   ├── main.cpp
│   │   ├── handlers.cpp
│   │   └── handlers.hpp
│   ├── message/       # Message Service
│   │   ├── main.cpp
│   │   ├── handlers.cpp
│   │   └── handlers.hpp
│   ├── common/        # Общий код
│   │   ├── models.cpp/hpp      # Модели данных
│   │   └── database.cpp/hpp    # Работа с БД
│   └── auth/          # JWT аутентификация
│       ├── jwt_auth_checker.cpp/hpp
│       └── jwt_auth_factory.cpp/hpp
├── configs/           # Конфигурации сервисов
├── tests/             # Интеграционные тесты
├── docker-compose.yaml
├── Dockerfile
├── nginx.conf
└── openapi.yaml       # OpenAPI спецификация
```

### Docker и развёртывание

Всё упаковано в Docker контейнеры. Один `docker-compose up` поднимает:
- 3 микросервиса
- Nginx как API Gateway
- Общую сеть для взаимодействия
- Shared volume для SQLite базы

Каждый сервис имеет health check, который проверяет доступность через `/ping` endpoint.

### Документация API

Создал полную OpenAPI 3.0 спецификацию в [`openapi.yaml`](openapi.yaml). Она включает:
- Описание всех endpoints
- Схемы request/response
- Примеры запросов
- Описание ошибок
- Security схему для JWT

Swagger UI доступен по адресу `http://localhost:8080/swagger/` — можно интерактивно тестировать API прямо из браузера.

### Тестирование

Написал 40+ интеграционных тестов на Python с pytest:

**User Service тесты:**
- Создание пользователя (успешное и с ошибками валидации)
- Проверка уникальности логина и email
- Логин (успешный, с неверными данными)
- Поиск по логину
- Поиск по маске имени

**Folder Service тесты:**
- Создание папок
- Получение списка папок
- Проверка прав доступа

**Message Service тесты:**
- Создание писем
- Получение списка писем в папке
- Получение письма по ID
- Проверка прав доступа к письмам

Все тесты используют реальные HTTP запросы к запущенным сервисам. Для изоляции каждый тест создаёт уникальных пользователей с timestamp и UUID в именах.
